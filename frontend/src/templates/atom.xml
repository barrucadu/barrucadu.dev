<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>barrucadu.dev events</title>
  <link href="{{ base_uri|e }}" rel="self" />
  <link href="{{ base_uri|e }}" />
  <id>{{ base_uri|e }}</id>
  <author>
    <name>Michael Walker</name>
    <email>mike@barrucadu.co.uk</email>
  </author>
  {% if feed_date is defined %}
  <updated>{{ feed_date|e }}</updated>
  {% endif %}
  {% for event in events %}
  <entry>
    <title>{{ event.project.name|e }}{% if event.tag is defined %} @ {{ event.tag|e }}{% endif %}: {{ event.status|e }}</title>
    {% if event.detailsUrl is defined %}<link href="{{ event.detailsUrl|e }}" />{% endif %}
    <id>{{ event.uuid|e }}</id>
    <published>{{ event.timestamp|e }}</published>
    <updated>{{ event.timestamp|e }}</updated>
    <summary type="html">
      <![CDATA[
{{ event.description }}
      ]]>
    </summary>
  </entry>
  {% endfor %}
</feed>
